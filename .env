# ── Database ──────────────────────────────────────────────────────────────────
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=zk_attendance
DB_USERNAME=zk_user
DB_PASSWORD=Juve1897@@

# Root password (used only by MySQL container, not the app)
MYSQL_ROOT_PASSWORD=Juve1897@@

# ── Application ───────────────────────────────────────────────────────────────
APP_PORT=8012

# Bootstrap admin API key — full access without a DB entry.
# Generate with: openssl rand -hex 40
# IMPORTANT: change this before deploying to production.
API_ADMIN_KEY=c50adeb09a12814c2d231b31838bf6814720a24963a956b2ec16e7b01ba6650453dc84e08d90e0fa

# ── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins for the REST API.
# Use * to allow all origins (fine for internal networks).
# Example: http://hrapp.company.local,https://dashboard.company.com
CORS_ALLOWED_ORIGINS=*
